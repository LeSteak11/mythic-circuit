export default function SettingsScreen() {
  return (
    <section aria-labelledby="settings-heading">
      <h2 id="settings-heading">Settings</h2>
      <p>Settings, including local data reset, will appear here.</p>
    </section>
  );
}
